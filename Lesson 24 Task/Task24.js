function multiply(length){
    // multiply length and width return the equivalent dollar value
    // conversion rate: 1800 naira to one dollar
    const width = length * 400;
    return width;
}

const areaOfRectangle = multiply(10)

console.log(areaOfRectangle);